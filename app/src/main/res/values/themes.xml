<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window theme used before Compose composes. Real theming is Material 3
         Expressive in DoseTheme. DayNight avoids a light flash in dark mode. -->
    <style name="Theme.Dose" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- core-splashscreen: shown until the first frame, then hands off to Theme.Dose. -->
    <style name="Theme.Dose.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_monochrome</item>
        <item name="postSplashScreenTheme">@style/Theme.Dose</item>
    </style>
</resources>
